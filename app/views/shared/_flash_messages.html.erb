<% flash.each do |type, message| %>
  <div role="alert" class="alert <%= bootstrap_class_for(type) %> alert-dismissable">
    <button class="close" data-dismiss="alert" aria-label="close"><span aria-hidden="true">&times;</span></button>
    <%= message %>
  </div>
<% end %>
